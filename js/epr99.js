/**
 * Epr 99
 *
 */

// import {fopen} from './utilities.js'
// import {Decimal} from '../libraries/decimals.js'
// let init = new Decimal(1)
//
// let a_power_b = (a, b) => {
//     let a_bigint = new Decimal(a)
//     let b_bigint = new Decimal(b)
//     return a_bigint.pow(b_bigint)
// }
//
//
// fopen((nums) => {
//     console.log(nums.split('\n').map((num, index) => {
//
//         let split_num = num.split(',').map(split => parseInt(split))
//         return [a_power_b(split_num[0], split_num[1]), index]
//
//     }).sort((a, b) => (a[0].lt(b[0])) ? -1 : ((a[0].gt(b[0])) ? 1 : 0)))
// })
//
